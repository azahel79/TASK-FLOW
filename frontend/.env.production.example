# Production backend API base URL.
# If frontend and backend share the same domain, use /api.
# If they are deployed separately, use https://your-backend-domain.com/api
VITE_API_BASE_URL="https://task-flow-1-a7k0.onrender.com/api"
